<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ggirick.gardening_back.mappers.notification.NotificationMapper">
    <!-- 알림 삽입 -->
    <insert id="insertNotification">
        INSERT INTO notification(id, user_uid, type, reference_id, message)
        VALUES(notification_seq.nextval, #{userUid}, #{type}, #{referenceId}, #{message})
    </insert>

    <!-- 알림 읽음 처리 -->
    <update id="updateIsReadById">
        UPDATE notification SET is_read = 'Y' WHERE id=#{id}
    </update>

    <!-- 알림 삭제 -->
    <delete id="deleteNotificationById">
        DELETE FROM notification WHERE id=#{id}
    </delete>

    <!-- 사용자에 따른 알림 목록 조회 -->
    <select id="getNotificationListByUserUid" resultType="com.ggirick.gardening_back.dto.notification.NotificationDTO">
        SELECT * FROM notification WHERE user_uid=#{userUid} ORDER BY created_at ASC
    </select>
</mapper>