<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ggirick.gardening_back.mappers.potList.PotListBookmarkMapper">
    <resultMap id="PotListingResultMap" type="com.ggirick.gardening_back.dto.potList.PotListDetailDTO">
        <id property="id" column="ID"/>
        <result property="title" column="TITLE"/>
        <result property="description" column="DESCRIPTION" jdbcType="CLOB"/>
        <result property="writerUid" column="WRITER_UID"/>
        <result property="type" column="TYPE" javaType="com.ggirick.gardening_back.enums.potList.PotListType"/>
        <result property="status" column="STATUS" javaType="com.ggirick.gardening_back.enums.potList.PotListStatus"/>
        <result property="price" column="PRICE"/>
        <result property="location" column="LOCATION"/>
        <result property="thumbnail" column="THUMBNAIL"/>
        <result property="viewCount" column="VIEW_COUNT"/>
        <result property="createdAt" column="CREATED_AT"/>
        <result property="updatedAt" column="UPDATED_AT"/>
        <result property="bumpedAt" column="BUMPED_AT"/>
        <result property="writerName" column="WRITER_NAME"/>
        <result property="bookmarkCount" column="BOOKMARK_COUNT"/>
        <result property="chatroomCount" column="CHATROOM_COUNT"/>
    </resultMap>

    <!--    분양글 찜-->
    <insert id="bookmarkPot">
        INSERT
        INTO pot_listing_bookmark(id, pot_listing_id, user_uid, created_at)
        VALUES(pot_listing_bookmark_seq.nextval, #{id}, #{userUid}, sysdate)
    </insert>

    <!--    분양글 찜 해제-->
    <delete id="unBookmarkPot">
        DELETE pot_listing_bookmark WHERE pot_listing_id=#{id} AND user_uid=#{userUid}
    </delete>

    <!--    사용자에 따른 찜 목록 조회-->
    <select id="getBookmarksByUserId" resultMap="PotListingResultMap">
        SELECT DISTINCT
        pl.id,
        pl.title,
        pl.writer_uid,
        pl.type,
        pl.status,
        pl.price,
        pl.location,
        pl.thumbnail,
        pl.view_count,
        pl.created_at,
        pl.updated_at,
        pl.bumped_at,
        ui.nickname AS writer_name,
        (SELECT COUNT(*) FROM chatroom WHERE pot_listing_id = pl.id) AS chatroom_count,
        (SELECT COUNT(*) FROM pot_listing_bookmark WHERE pot_listing_id = pl.id) AS bookmark_count
        FROM pot_listing_bookmark pb
        JOIN pot_listing pl ON pl.id = pb.pot_listing_id
        JOIN user_info ui ON ui.uuid = pl.writer_uid
        LEFT JOIN pot_listing_tag_mapping ptm ON ptm.pot_listing_id = pl.id
        LEFT JOIN plant_tag pt ON pt.tag_id = ptm.plant_tag_id
        WHERE pb.user_uid = #{userUid}
        ORDER BY pl.id DESC
    </select>
</mapper>